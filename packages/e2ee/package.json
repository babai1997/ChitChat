{
  "name": "@chitchat/e2ee",
  "version": "0.1.0",
  "private": true,
  "description": "X3DH + Double Ratchet E2EE protocol implementation for ChitChat, built on audited @noble primitives. Consumed directly as TypeScript source (no build step) by frontend and mobile via path alias, same pattern as @chitchat/types.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/ciphers": "^1.0.0",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
